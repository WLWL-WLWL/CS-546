<h1>Characters Found</h1>
<h2>Search Term: {{searchTerm}}</h2>
<main>
    {{#if characters.length}}
    <ul>
        {{#each characters}}
        <li><a href="/characters/{{this.id}}">{{this.name}}</a></li>
        {{/each}}
    </ul>
    {{else}}
    <p class="not-found">We're sorry, but no results were found for {{searchTerm}}.</p>
    {{/if}}
    <a class="back" href="/">Make another search</a>
</main>